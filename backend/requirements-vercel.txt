# Vercel-optimized requirements (lightweight for serverless)
# Production dependencies only - no dev tools

# Core framework
fastapi==0.115.0
uvicorn==0.30.0
pydantic==2.8.2

# Database (with connection pooling)
SQLAlchemy==2.0.36
alembic==1.13.2
psycopg2-binary==2.9.9

# Authentication
passlib[bcrypt]==1.7.4
PyJWT==2.9.0
pyotp==2.9.0
authlib==1.3.1

# Utilities
python-multipart==0.0.9
email-validator==2.2.0
httpx==0.27.2
python-dotenv==1.0.1
python-dateutil==2.8.2
pytz==2023.3

# Media handling
Pillow==10.1.0
qrcode==7.4.2
boto3==1.34.158

# Caching
redis[hiredis]==5.0.1

# Push notifications
pywebpush==1.14.0

# Payments
stripe==7.7.0

# Validation
jsonschema==4.23.0

# ASGI server (required for Vercel)
starlette==0.40.0
